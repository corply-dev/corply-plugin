interface:
  display_name: "Corply"
  short_description: "Incorporate your company through conversation."
  brand_color: "#10A37F"
  default_prompt: "Use $corply to continue my company's incorporation."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "corply"
      description: "Corply company incorporation MCP server"
      transport: "streamable_http"
      url: "https://corply.dev/mcp"
